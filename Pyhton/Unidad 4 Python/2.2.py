numUno = float(input("Ingrese el num uno"))
numDos = float(input("Ingrese el num dos"))

if numUno > numDos:
  print(f"Num {numUno} es mayor que {numDos}", )
else:   
  print(f"Num {numDos} es mayor que {numUno}", )

